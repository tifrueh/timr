# timr

`timr` is a very small shell script for the Linux terminal. It can be used to
set a timer, which is then displayed on the CLI. As soon as the timer runs out,
a notification is dispatched using `notify-send`. For more information on usage,
execute the script with the `--help` flag.
